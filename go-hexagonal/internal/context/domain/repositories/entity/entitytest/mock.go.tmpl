package {{ .Entity | lower }}repotest

import "{{ .ModulePath }}/internal/{{ .Context }}/domain/repositories/{{ .Entity | lower }}repo"

// Mock{{ .Entity }}Repository is a function-based mock for unit tests.
// Methods are generated by go-surgeon — run:
//   go-surgeon mock --interface {{ .ModulePath }}/internal/{{ .Context }}/domain/repositories/{{ .Entity | lower }}repo.{{ .Entity }}Repository --mock_name Mock{{ .Entity }}Repository --file <this file>
type Mock{{ .Entity }}Repository struct{}

// Compile-time check.
var _ {{ .Entity | lower }}repo.{{ .Entity }}Repository = (*Mock{{ .Entity }}Repository)(nil)
