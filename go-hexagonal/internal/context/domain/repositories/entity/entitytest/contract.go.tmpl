package {{ .Entity | lower }}repotest

import (
	"testing"

	"{{ .ModulePath }}/internal/{{ .Context }}/domain"
	"{{ .ModulePath }}/internal/{{ .Context }}/domain/repositories/{{ .Entity | lower }}repo"
)

// {{ .Entity }}ContractTesting is the shared contract suite for {{ .Entity }}Repository.
// Both the mock and the real adapter run this same function, so if the mock says
// "Create stores and FindByID retrieves" then the real adapter must too.
//
// The suite is passed an `actor` (the scope every method runs under). To exercise
// the IDOR / cross-scope cases, build a second actor with a different scope inside
// the relevant t.Run. Adjust the cases to the methods you actually keep on the
// interface — see the `add_repository_interface` hint for the full checklist.
func {{ .Entity }}ContractTesting(t *testing.T, repo {{ .Entity | lower }}repo.{{ .Entity }}Repository, actor *domain.Actor) {
	t.Run("Contract: Create stores and FindByID retrieves", func(t *testing.T) {
		// TODO(scaffolding): construct a new domain.{{ .Entity }}, call repo.Create, then
		// repo.FindByID with the same actor + id, and assert every field (including
		// timestamps within tolerance) round-trips. Required before the adapter proves
		// it persists real data.
		t.Fatal("TODO(scaffolding): implement this test case or remove the t.Run block entirely — do not leave placeholders")
	})

	t.Run("Contract: FindByID out-of-scope returns error", func(t *testing.T) {
		// IDOR contract: reading an entity through an actor in a *different* scope
		// MUST return an error, not the entity.
		// TODO(scaffolding): seed an entity under `actor`, build a second actor in
		// another scope, call FindByID(other, sameID) and assert Err{{ .Entity }}NotFound.
		// This is the single test that proves scope isolation at the adapter layer.
		t.Fatal("TODO(scaffolding): implement this test case or remove the t.Run block entirely — do not leave placeholders")
	})

	t.Run("Contract: List returns only items in scope", func(t *testing.T) {
		// TODO(scaffolding): seed items under two actors in different scopes; call List
		// with one of them; assert the result contains only that actor's items.
		// Pagination is the most common scope-leak point — keep this case.
		t.Fatal("TODO(scaffolding): implement this test case or remove the t.Run block entirely — do not leave placeholders")
	})

	t.Run("Contract: Delete removes item", func(t *testing.T) {
		// TODO(scaffolding): create, delete, then FindByID and assert Err{{ .Entity }}NotFound.
		// Also call Delete a second time and assert the same error (idempotent contract).
		t.Fatal("TODO(scaffolding): implement this test case or remove the t.Run block entirely — do not leave placeholders")
	})

	t.Run("Contract: FindByID not found returns error", func(t *testing.T) {
		// TODO(scaffolding): call FindByID with a freshly generated id that was never
		// persisted; assert Err{{ .Entity }}NotFound. Required so callers can rely on
		// errors.Is for not-found, instead of nil-checking the return value.
		t.Fatal("TODO(scaffolding): implement this test case or remove the t.Run block entirely — do not leave placeholders")
	})
}
