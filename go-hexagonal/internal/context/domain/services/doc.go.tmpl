// Package services contains pure domain logic — business rules, calculations,
// and policies that don't require I/O. No interfaces, no ports.
//
// For usecase ports (Commands/Queries), see domain/usecases/.
// For repository ports, see domain/repositories/.
// For publisher ports, see domain/publishers/.
package services
