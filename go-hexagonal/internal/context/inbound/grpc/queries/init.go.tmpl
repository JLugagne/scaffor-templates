package queries

// Per-entity gRPC query handlers define their own handler structs
// with usecase interfaces as dependencies. No shared Service struct needed.
// See entity.go files for the per-entity pattern.
