package app_test

// Contract tests verify the app service works correctly with REAL repositories.
// Generate test skeletons with go-surgeon:
//   go-surgeon test --file internal/{{ .Context }}/app/{{ .Entity | lower }}.go --identifier App.Create{{ .Entity }}
//
// Then adapt the generated test to use real repos via testcontainers instead of mocks.
